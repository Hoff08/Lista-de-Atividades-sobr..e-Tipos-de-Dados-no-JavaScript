// Declare a variável vazia
let vazia = null;

// Verifique se vazia é igual a undefined (==) e estritamente igual a undefined (===)
console.log(vazia == undefined); // Saída: true
console.log(vazia === undefined); // Saída: false

// Declare a variável naoDefinida
let naoDefinida;

// Verifique se naoDefinida é igual a undefined (==) e estritamente igual a undefined (===)
console.log(naoDefinida == undefined); // Saída: true
console.log(naoDefinida === undefined); // Saída: true
